<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.drmednotifier">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:alwaysRetainTaskState="true"
        android:exported="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Edit_Medication"
            android:screenOrientation="portrait" />
        <activity android:name=".New_User_Profile"
            android:screenOrientation="portrait" /> <!-- <activity android:name=".Frag_Setting"></activity> -->
        <activity android:name=".Dose_Page"
            android:screenOrientation="portrait" />
        <activity android:name=".Medicine_List" /> <!-- <activity android:name=".Frag_Notification" /> -->
        <activity android:name=".Nav_page"
            android:screenOrientation="portrait" />
        <activity android:name=".Second_page_get_personaldata"
            android:screenOrientation="portrait" />
        <activity android:name=".Setting_Page"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Popup_Window"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RingActivity"
            android:screenOrientation="portrait" />

        <receiver android:name=".broadcastreceiver.AlarmBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name=".service.AlarmService" />
        <service android:name=".service.RescheduleAlarmsService" />
        <service android:name=".service.DescheduleAlarmsService" />
    </application>

</manifest>